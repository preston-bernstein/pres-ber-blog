# Context Snapshot: 2026 Content Refresh Pass

## Project shape
```
_redirects  amplify.yml  assets  config  content  data  docs  hugo.toml
i18n  images  jasmine.json  layouts  LICENSE  netlify.toml  package.json
playwright.config.js  postcss.config.js  readme.md  scripts  spec
tailwind.config.js  tests  theme.toml  themes  vercel-build.sh  vercel.json
```

## CLAUDE.md
none

## CONTEXT.md
none

## ADRs (titles only)
none (no docs/adr/ directory)

## Repo-specific notes not in CLAUDE.md/CONTEXT.md (gathered this session)
- Deploy = push to `main` triggers `.github/workflows/main.yml` (GitHub
  Actions FTP deploy to prestonbernstein.com). No staging.
- Hugo pinned to 0.164.0 in CI; local Homebrew Hugo confirmed at 0.164.0 too
  (checked 2026-08-10, can drift on a Homebrew upgrade — recheck before
  build validation next time).
- Theme is Blowfish (`themes/blowfish`, git submodule) — migrated from
  Hugoplate earlier today (2026-08-10); submodule was NOT checked out on
  fresh clone, needed `git submodule update --init --recursive`.
- `content/english/blog/` has 27 files: 25 real posts, `_index.md` (blog
  listing page, not a post), and `native-hugo-image-pipeline-webp-lqip-and-mermaid.md`
  (today's image-pipeline demo post).
- Image pipeline: `layouts/_default/_markup/render-image.html` (WebP/srcset/
  LQIP) and `layouts/_default/_markup/render-codeblock-mermaid.html`
  (Mermaid from fenced code blocks) — both live, both demonstrated in the
  reference post above.
- `themes/blowfish/layouts/partials/article-meta/basic.html` confirmed
  (2026-08-10) to render `.Date` by default in the blog list; `.Lastmod`
  only shows if `showDateUpdated` is explicitly true (default false, site
  config not changing it).
- Existing Playwright suite: `tests/e2e/{blog-post-content,dark-mode,
  image-pipeline,mermaid,reading-progress-bar,toc-scrollspy}.spec.js`.
- Prior passes' audit-trail precedent: `docs/markup-2026-baseline/audit-log-group{A,B,C,demo}.md`.
